import java.util.*;

//we will use interator for iterating 
//it is inbuild in util package 
//same can be used for hashMap

//has two functions
// 1).next() =>move on two next
// 2)hasnext() =>return true or false if exist or not
// firstly interator will point to null position 
public class Hash2 {
    public static void main(String[] args) {
        HashSet<Integer> set=new HashSet<>();

        set.add(1);
        set.add(2);
        set.add(3);
        set.add(5);
        System.out.println();
    //     Iterator i=set.iterator();
    //     while(i.hasNext()){
    //         System.out.print(i.next()+"=>");
    //     }
    }
}
