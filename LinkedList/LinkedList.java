package LinkedList;

public class LinkedList{
    //This code is to form the new node
    public class Node{  
        int data;   //node contain data
        Node next;  //and next reference variable

        //initialzed with the help of the constructor
        public Node(int data){
            this.data=data;
            this.next=null;

        }
    }
    
    public static void main(String args[])
    {

    }
}

//the first node is know as the head and last node is know as the tail